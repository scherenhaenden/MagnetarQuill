import { ImageModalComponentModel } from './image-modal-component-model';

describe('ImageModalComponentModel', () => {
  it('should create an instance', () => {
    expect(new ImageModalComponentModel()).toBeTruthy();
  });
});

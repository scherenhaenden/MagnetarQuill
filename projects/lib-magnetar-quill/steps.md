# LibMagnetarQuill

This library is part of an Nx workspace.

## Code scaffolding

Run `nx g @nx/angular:component component-name --project=lib-magnetar-quill` to generate a new component. You can also use `nx g @nx/angular:directive|pipe|service|class|guard|interface|enum|module --project=lib-magnetar-quill`.

## Build

Run `nx build lib-magnetar-quill` to build the project. The build artifacts will be stored in the `dist/` directory.

## Publishing

After building your library with `nx build lib-magnetar-quill`, go to the dist folder `cd dist/lib-magnetar-quill` and run `npm publish`.

## Running unit tests

Run `nx test lib-magnetar-quill` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Further help

To get more help on Nx use `nx help` or go check out the [Nx Documentation](https://nx.dev/angular) page.
